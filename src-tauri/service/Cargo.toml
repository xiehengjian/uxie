[package]
name = "service"
version = "0.1.0"
edition = "2021"

[dependencies]
common = {path="../common"}
sea-orm = {version = "0.12.3",features = ["sqlx-sqlite","runtime-tokio-native-tls","macros"]}
